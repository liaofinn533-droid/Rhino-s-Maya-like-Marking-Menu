# Rhino 标记菜单 · 项目阶段总结

> 项目已迭代到 **v7.2（可用版）**，当前功能完整、可日常使用。
> 本文件记录项目进度、当前能力与后续方向；如需继续修改，另行再议。

---

## 一、项目是什么

个人自用的 **Rhino 8 建模提速工具**：仿 Maya 标记菜单、参考 RS2 文字菜单交互的**两级选择菜单**。
纯 **Python** 实现（用 Rhino 8 自带运行环境，无需安装任何额外软件），由命令行 `_MM` 或快捷键呼出。

**核心交互（当前版）：**

- **第一级**：中心一圈"工具集"（曲线、圆、矩形、实体工具、变动…）。
- **第二级**：选中某个工具集后，它以自己为圆心生成一圈**圆形工具标签环**，再点/松手选择具体工具。
- 选中后，其它工具集自动**变暗到约 10%**，画面干净不抢焦点。

---

## 二、迭代历程（到本阶段为止）

| 版本 | 阶段内容 |
|---|---|
| v2 / v3 | 早期仿 Maya 扇形标记菜单（备份于 `backup/`） |
| v6 | 两级菜单：工具集 → 具体工具，外圈扇形双层展开 |
| **v7** | **重构为"两级 · 圆形标签环"**：选中工具集后以其为圆心生成圆形工具环，取代扇形展开（本阶段最大改动） |
| **v7.1** | 选中效果：其它工具集自动变暗到约 10%，支持点变暗项直接切换工具集 |
| **v7.2** | 新增"**按住 Ctrl 呼出、松手执行**"模式（配合键盘快捷键使用），保留原点击模式 |

---

## 三、当前功能清单（已可以使用）

- **两种呼出方式**
  - 点击模式：命令行输入 `_MM`（或 `MM`）。
  - 按住 Ctrl 模式：键盘快捷键 `Ctrl+Alt+M`，按住呼出、松手提交（悬停选中、松手执行）。
- **两级选择**
  - 一级：环形工具集；二级：以选中工具集为圆心的圆形工具环。
  - 松手/点击判定：高亮工具 → 执行；停在工具集方框 → 执行默认工具；空白 → 取消。
- **高度可配置**：工具集/工具的名称、命令、颜色、提示、排列位置全部由 `config.json` 控制，改完重新呼出即生效。
- **稳健性**
  - 49 个工具全部可选中、无重叠、不越出窗口（几何模拟验证通过）。
  - 支持带选项的命令写法（如 `Rectangle _3Point`、`Circle _Diameter`）。
  - 多显示器/缩放下自动夹紧在屏幕内；ESC / 失焦均可关闭。
  - `config.json` 损坏时自动回退内置默认配置，不崩溃。

---

## 四、项目文件结构（已统一分包）

```
rhino-marking-menu/                 ← 项目根文件夹
├── marking_menu.py                 ← 主程序（运行核心，Rhino 别名指向这里）
├── config.json                     ← 配置（与主程序同目录，运行必需）
├── README.md                       ← 安装 / 使用 / 配置说明（入口文档）
├── 阶段总结.md                      ← 本文件（项目阶段总结）
├── preview/                        ← 界面效果预览
│   ├── preview.png
│   └── menu_preview_v7.html
├── backup/                         ← 旧版本备份
│   ├── marking_menu_v2_backup.py
│   ├── marking_menu_v3_backup.py
│   └── config_v6_backup.json
└── tiles/                          ← 参考素材（Rhino 默认工具面板截图切片）
```

> 说明：`marking_menu.py` 与 `config.json` 必须保持同目录（程序按同目录加载配置），因此作为"运行核心"放在根目录；别名路径不受本次整理影响。

---

## 五、安装 / 使用入口（摘要）

1. **别名**：`工具 → 选项 → 别名`，`MM` → `_-RunPythonScript "F:\cod\Rhino-s-Maya-like-Marking-Menu\marking_menu.py"`
2. **快捷键（可选）**：`工具 → 选项 → 键盘`，`Ctrl+Alt+M` → 同一行脚本。
3. 详细步骤见 `README.md`。

---

## 六、已知事项与注意

- 工具命令名来自"识图辨认 + Rhino 标准对照"的最佳猜测，个别命令如与 Rhino 默认面板不符，改 `config.json` 里的 `tools[].command` 即可。
- 改 `config.json` 前建议先备份（标点写错会回退默认配置）。
- 代码文件内的版本号注释仍为 v7（不影响使用），本阶段累计增强到 v7.2。

---

## 七、后续方向（暂缓，等之后再改）

- 用 AutoHotkey 绑鼠标侧键呼出。
- 二级环支持更多层级 / 返回上一级交互。
- 按 Rhino 默认工具栏补齐更多工具集（可对照 `tiles/` 素材逐项完善）。
- 将来需要公开分发时，再评估迁移 C#。

---

*阶段总结生成日期：2026-08-25*